Open a wayland window (gui) with an open pipe which takes input

Test to put a wayland screen with an open pipe which can show all text put into the pipe.
Like a clone of yad but much more faster and lacks of alla yads functions.

i use kde kwin to arrange this screen where it should be placed and how because of wayland rules.
